apiVersion: v1
kind: Pod
metadata:
  namespace: poc
  labels:
    run: nginx
  name: nginx
spec:
  containers:
  - image: nginx
    name: nginx
    resources:
      limits:
        cpu: 1
  dnsPolicy: ClusterFirst
  restartPolicy: Always
